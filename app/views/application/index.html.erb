<ul>
  <% @groups.each do |group| %>
    <li><%= group.name %></li>
    <% unless @current_user && @current_user.groups.include?(group) %>
      <%= button_to 'Join', "/groups/#{group.id}/user_groups/new", method: :get %>
    <% end %>
  <% end %>
</ul>