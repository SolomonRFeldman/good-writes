<%= form_with model: @group do |f| %>
  <%= f.text_field :name %>
  <%= f.select :form, options_for_select([['Poetry'], ['Fiction'], ['Non-Fiction']]) %>
  <%= text_field :user_group, :alias %>
  <%= f.submit "Create Group" %>
<% end %>